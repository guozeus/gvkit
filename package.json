{
  "name": "gvkit",
  "version": "0.1.1",
  "description": "Format selected text with blue or purple text and background colors.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "node --experimental-strip-types --test tests/*.test.ts"
  },
  "private": true,
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^22.15.17",
    "esbuild": "0.25.5",
    "obsidian": "^1.13.1",
    "typescript": "^5.8.3"
  }
}
